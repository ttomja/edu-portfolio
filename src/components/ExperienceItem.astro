---
interface Props {
  role: string;
  company: string;
  period: string;
  description: string;
  skills: string[];
  index: number;
}

const { role, company, period, description, skills, index } = Astro.props;
const isEven = index % 2 === 0;
const number = (index + 1).toString().padStart(2, "0");
---

<div class={`relative flex flex-col md:flex-row gap-8 md:gap-16 items-start py-12 md:py-16 ${!isEven ? "md:flex-row-reverse" : ""}`}>
  <div class={`absolute top-2 ${isEven ? "right-0 md:left-0" : "left-0 md:right-0"} text-[6rem] md:text-[8rem] leading-none font-bold text-white/[0.03] select-none pointer-events-none -translate-y-1/4`}>
    {number}
  </div>

  <div class="hidden md:block absolute left-1/2 top-0 bottom-0 w-px bg-gradient-to-b from-transparent via-white/10 to-transparent -translate-x-1/2"></div>
  <div class="hidden md:block absolute left-1/2 top-20 w-3 h-3 rounded-full bg-indigo-500 border-4 border-neutral-950 -translate-x-1/2 shadow-[0_0_20px_rgba(99,102,241,0.45)]"></div>
  <div class="hidden md:block w-1/2"></div>

  <div class="w-full md:w-1/2 relative group">
    <div class="relative overflow-hidden bg-neutral-900/45 border border-white/10 p-6 md:p-8 rounded-2xl backdrop-blur-xl transition-all duration-300 hover:-translate-y-1 hover:bg-neutral-900/70 hover:border-indigo-400/25">
      <div class="pointer-events-none absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-white/20 to-transparent"></div>
      <div class="flex flex-col sm:flex-row sm:items-start justify-between gap-4 mb-6">
        <div>
          <h3 class="text-2xl font-bold text-white group-hover:text-indigo-300 transition-colors">
            {role}
          </h3>
          <p class="text-lg text-indigo-400 font-medium mt-1">{company}</p>
        </div>
        <span class="inline-flex px-4 py-1.5 text-xs font-mono font-bold text-neutral-300 bg-white/5 rounded-full border border-white/10 self-start">
          {period}
        </span>
      </div>

      <p class="text-neutral-400 mb-8 leading-relaxed text-lg border-l-2 border-white/10 pl-4 group-hover:border-indigo-400/35 transition-colors">
        {description}
      </p>

      <div class="flex flex-wrap gap-2">
        {
          skills.map((skill) => (
            <span class="px-3 py-1 text-xs font-medium text-neutral-400 bg-neutral-950 border border-white/5 rounded-md group-hover:border-white/10 transition-colors">
              {skill}
            </span>
          ))
        }
      </div>
    </div>
  </div>
</div>
