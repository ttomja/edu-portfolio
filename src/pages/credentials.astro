---
// =============================================================================
// CREDENTIALS PAGE
// =============================================================================
// Displays all certifications and education in a timeline format.
// Data is loaded from: src/data/siteData.ts -> credentials, sectionContent
// =============================================================================

import Layout from "../layouts/Layout.astro";
import TimelineItem from "../components/TimelineItem.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import { credentials, sectionContent } from "../data/siteData";

// Group credentials by year
const groupedCredentials = credentials.reduce(
  (acc, cred) => {
    if (!acc[cred.year]) acc[cred.year] = [];
    acc[cred.year].push(cred);
    return acc;
  },
  {} as Record<string, typeof credentials>,
);

// Sort years descending to show newest credentials first
const sortedYears = Object.keys(groupedCredentials).sort(
  (a, b) => parseInt(b) - parseInt(a),
);
---

<Layout>
  <Navbar />

  <div class="pt-32 pb-12 px-6">
    <div class="scroll-fade max-w-6xl mx-auto text-center">
      <h1 class="text-5xl md:text-6xl font-bold text-white mb-6">
        {sectionContent.education.title}
      </h1>
      <p class="text-neutral-400 max-w-2xl mx-auto text-lg">
        {sectionContent.education.subtitle}
      </p>
    </div>
  </div>

  <section class="pt-24 pb-32 relative overflow-hidden">
    <div class="max-w-6xl mx-auto px-6">
      <div class="scroll-fade">
        <div class="flex flex-col relative">
          <div
            class="absolute left-1/2 top-0 bottom-0 w-px bg-gradient-to-b from-neutral-800 via-neutral-800 to-transparent hidden md:block -translate-x-1/2"
          >
          </div>

          {
            sortedYears.map((year, yearIndex) => (
              <div class="mb-24 relative">
                {/* Year Marker on Spine */}
                <div class="flex justify-center items-center mb-12 relative z-10">
                  <div class="bg-neutral-900 border border-neutral-700 px-6 py-2 rounded-full text-indigo-400 font-bold font-mono text-xl shadow-xl">
                    {year}
                  </div>
                </div>

                <div class="space-y-0">
                  {groupedCredentials[year].map((cred) => (
                    <TimelineItem
                      title={cred.title}
                      issuer={cred.issuer}
                      year={cred.year}
                      link={cred.link}
                      imagePath={cred.imagePath}
                      index={yearIndex}
                    />
                  ))}
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <a
    href="/"
    class="fixed bottom-4 right-4 md:bottom-8 md:right-8 z-50 flex items-center gap-2 px-4 py-2.5 md:px-5 md:py-3 bg-neutral-900/80 backdrop-blur-md border border-white/10 rounded-full text-white shadow-2xl hover:bg-neutral-800 hover:scale-105 transition-all duration-300 group"
  >
    <svg
      class="w-4 h-4 text-neutral-400 group-hover:text-white transition-colors group-hover:-translate-x-1 duration-300"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
    </svg>
    <span class="font-medium">Back to Home</span>
  </a>

  <Footer />
</Layout>
